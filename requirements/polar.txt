# External dependency of the polar extension (import name: graph_longrange).
# Pinned by commit: an unpinned git dependency can break CI on its own.
# To bump: update the SHA and run the polar jobs (ci-extensions.yaml).
git+https://github.com/WillBaldwin0/graph_electrostatics.git@fbb3a6f1c76b69a55dd321c31c319baf0f534571
